# Task Management System

Система управління завданнями (Task Management System) - це веб-додаток для створення, перегляду та управління завданнями різних типів. Додаток побудований на TypeScript з використанням Vite, json-server та сучасних веб-технологій.

## 🚀 Особливості

- ✅ Створення завдань різних типів (Task, Subtask, Bug, Story, Epic)
- 📋 Перегляд списку всіх завдань
- ✏️ Відображення детальної інформації про завдання
- 🗑️ Видалення завдань
- 🎨 Сучасний та адаптивний інтерфейс
- 🔄 Реалізація через REST API (json-server)
- 🧪 Покриття тестами (Vitest)

## 📋 Типи завдань

Додаток підтримує 5 типів завдань:

1. **Task** - Звичайне завдання
2. **Subtask** - Підзавдання (з прив'язкою до батьківського завдання)
3. **Bug** - Баг/помилка (з кроками відтворення, очікуваним/фактичним результатом, серйозністю)
4. **Story** - Історія користувача (з story points, критеріями прийняття, призначеним виконавцем)
5. **Epic** - Епік (з пов'язаними історіями, цілями, бізнес-цінністю)

## 🛠️ Технології

- **TypeScript** - типізована мова програмування
- **Vite** - швидкий інструмент збірки та розробки
- **json-server** - REST API для мокування бекенду
- **Vitest** - фреймворк для тестування
- **HTML/CSS** - структура та стилізація інтерфейсу
- **Fetch API** - для HTTP запитів

## 📦 Встановлення

### Передумови

- Node.js (версія 14 або вище)
- npm або yarn

### Кроки встановлення

1. Клонуйте репозиторій або перейдіть до директорії проекту:
```bash
cd work4
```

2. Встановіть залежності:
```bash
npm install
```

## 🚀 Запуск додатку

### 1. Запустіть json-server (API сервер)

У одному терміналі:
```bash
npm run server
```

Сервер запуститься на `http://localhost:3000`

### 2. Запустіть dev сервер

У іншому терміналі:
```bash
npm run dev
```

Додаток буде доступний на `http://localhost:5173`

## 📖 Використання

### Створення завдання

1. Заповніть форму на головній сторінці:
   - **Назва** (обов'язкове поле) - назва завдання
   - **Опис** (опціонально) - детальний опис завдання
   - **Статус** - виберіть статус (To Do, In Progress, Виконано)
   - **Пріоритет** - виберіть пріоритет (Низький, Нормальний, Високий)
   - **Дедлайн** (опціонально) - дата завершення завдання

2. Натисніть кнопку "Створити завдання"

3. Завдання з'явиться в списку нижче форми

### Перегляд завдань

Всі створені завдання відображаються в списку на головній сторінці. Для кожного завдання показується:
- Назва та опис
- Тип завдання
- Статус
- Пріоритет
- Дата створення
- Дедлайн

### Видалення завдання

1. Знайдіть завдання, яке потрібно видалити
2. Натисніть кнопку "Видалити" в правому верхньому куті картки завдання
3. Підтвердіть видалення в діалоговому вікні

## 🧪 Тестування

Запуск тестів:
```bash
npm run test
```

Запуск тестів з UI:
```bash
npm run test:ui
```

Запуск тестів один раз:
```bash
npm run test:run
```

### Покриття тестами

Проект містить unit тести для:
- Моделей (Task, Subtask, Bug, Story, Epic)
- Сервісів (TaskService)
- Контролерів (TaskController)
- Утиліт (task.utils)

## 📁 Структура проекту

```
work4/
├── src/
│   ├── models/              # Моделі даних
│   │   ├── task.model.ts
│   │   ├── subtask.model.ts
│   │   ├── bug.model.ts
│   │   ├── story.model.ts
│   │   ├── epic.model.ts
│   │   └── *.test.ts        # Тести моделей
│   ├── modules/
│   │   └── tasks/
│   │       ├── task.types.ts        # Типи та інтерфейси
│   │       ├── task.service.ts      # Сервіс для роботи з API
│   │       ├── task.controller.ts   # Контролер для бізнес-логіки
│   │       └── *.test.ts           # Тести сервісів та контролерів
│   ├── utils/
│   │   ├── task.utils.ts           # Утиліти (форматування, лейбли)
│   │   └── *.test.ts              # Тести утиліт
│   ├── test/
│   │   └── setup.ts                # Налаштування тестового середовища
│   ├── main.ts                     # Точка входу додатку
│   └── style.css                   # Стилі додатку
├── public/                         # Статичні файли
├── db.json                         # База даних для json-server
├── index.html                      # HTML шаблон
├── package.json                    # Залежності та скрипти
├── tsconfig.json                   # Конфігурація TypeScript
├── vite.config.ts                  # Конфігурація Vite
└── README.md                       # Документація
```

## 🔧 Налаштування

### Зміна порту API сервера

Якщо потрібно змінити порт json-server, відредагуйте `src/modules/tasks/task.service.ts`:
```typescript
const API_BASE_URL = 'http://localhost:3000'; // Змініть на потрібний порт
```

### Зміна порту dev сервера

Відредагуйте `vite.config.ts` або використайте параметр `--port`:
```bash
npm run dev -- --port 3001
```

## 📝 API Endpoints

Додаток використовує наступні endpoints json-server:

- `GET /tasks` - отримати всі завдання
- `GET /tasks/:id` - отримати завдання за ID
- `POST /tasks` - створити нове завдання
- `PUT /tasks/:id` - оновити завдання
- `PATCH /tasks/:id` - частково оновити завдання
- `DELETE /tasks/:id` - видалити завдання

## 🎨 Стилізація

Додаток використовує сучасний градієнтний дизайн з адаптивним макетом. Стилі знаходяться в `src/style.css`.

## 🐛 Відомі проблеми

- Деякі тести моделей можуть падати через особливості TypeScript компіляції (5 з 35 тестів)
- Основна функціональність працює коректно

## 📄 Ліцензія

Цей проект створено в навчальних цілях.

## 👨‍💻 Розробка

Для розробки використовуйте:
- `npm run dev` - запуск dev сервера з hot reload
- `npm run build` - збірка проекту для production
- `npm run preview` - перегляд production збірки

## 📞 Підтримка

Якщо виникли питання або проблеми, перевірте:
1. Чи запущений json-server на порту 3000
2. Чи всі залежності встановлені (`npm install`)
3. Консоль браузера (F12) для діагностики помилок

---

**Приємного використання! 🎉**
